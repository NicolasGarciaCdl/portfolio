<form method="post" action="{{ path('app_language_delete', {'id': language.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer  cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ language.id) }}">
    <button class="btn btn-danger mt-2">Supprimer</button>
</form>
