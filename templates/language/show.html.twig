{% extends 'base.html.twig' %}

{% block title %}Language{% endblock %}

{% block body %}
    <h1 class="text-center">Language</h1>
    <div class="container">
        <table class="table">

            <tbody>
            <tr>
                <th>#</th>
                <td>{{ language.id }}</td>
            </tr>
            <tr>
                <th>Nom du langage</th>
                <td>{{ language.title }}</td>
            </tr>
            <tr>
                <th>Image du langage</th>
                <td>{{ language.imageName }}</td>
            </tr>

            </tbody>
        </table>

        <a class="btn btn-info" href="{{ path('app_language_index') }}">Retour à la liste</a>

        <a class="btn btn-success" href="{{ path('app_language_edit', {'id': language.id}) }}">Modifier</a>

        {{ include('language/_delete_form.html.twig') }}
    </div>
{% endblock %}
