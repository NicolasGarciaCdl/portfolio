{% extends 'base.html.twig' %}

{% block title %}Modifier un commentaire{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-6">
                <h1 class="text-center mt-5">Modifier un commentaire</h1>

                {{ include('comment/_form.html.twig', {'button_label': 'Modifier'}) }}

                <a class="btn btn-info" href="{{ path('app_comment_index') }}">Retour à la liste</a>

                {{ include('comment/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
